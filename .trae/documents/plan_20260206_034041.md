您描述的“最终形态”非常清晰：**AI 编剧 -> 智能分镜 -> 多角色/场景关联 -> 多模型并发生成 (对比选优) -> 视频生成**。我们目前正处于“多模型并发生成 (对比选优)”的关键阶段。

为了实现您要求的“只开放即梦和可灵，支持并发调用对比”，我将进行以下调整：

### 1. 配置调整 (`lib/ai-models.ts`)
*   **隐藏 Fal 模型**: 暂时注释掉 Fal 相关的配置，确保 UI 上只显示 Jimeng 和 Kling。
*   **确保可用性**: 确认 Jimeng 和 Kling 的配置已就绪。

### 2. UI 优化 (`StoryboardFrame.tsx`)
*   **默认全选**: 为了方便您进行“同时生成，对比效果”，我将修改默认选中逻辑，默认勾选列表中的所有模型（即梦 + 可灵）。
*   **轮播图优化**: 确保轮播图能清晰展示图片的来源模型（例如在图片角标显示 "Jimeng V4" 或 "Kling V1"），方便您直观对比。

### 3. 验证基础流程
本次调整后，您将可以进行完整的“现阶段测试”：
1.  **AI 剧本**: 输入故事，自动生成分镜。
2.  **资产关联**: 为分镜选择多个角色/场景。
3.  **并发生成**: 点击生成，Jimeng 和 Kling 同时工作。
4.  **对比选优**: 在轮播图中查看两者的结果，选择一张作为定稿。

### 后续规划 (视频生成)
*   正如您所说，视频生成是下一步。目前的架构已经预留了 `video` 类型的模型配置。
*   一旦图片生成流程跑通，我们只需添加 `kling-video` 或 `jimeng-video` 的 Provider，即可复用现有的“并发对比”逻辑来实现视频生成。

请确认是否开始调整模型配置？